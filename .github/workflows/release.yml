name: Release
on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      job:
        description: "Job to run"
        required: true
        default: "build-thunderstore-package"

permissions:
  contents: write # Needed to write to GitHub draft release

env:
  # NORTHSTAR_VERSION: v1.31.3
  GIT_VERSION: ${{ github.ref_name }}
  # ION_PATCH: 1
  DISCORD_RPC_VERSION: v14

jobs:
  build-northstar:
    runs-on: ubuntu-22.04
    outputs:
      sha512: ${{ steps.checksum.outputs.sha512 }}
    steps:
      - name: Set version variables
        run: |
          echo "NORTHSTAR_VERSION=${GITHUB_REF_NAME%_*}" >> $GITHUB_ENV
          echo "ION_PATCH=${GITHUB_REF_NAME##*_}" >> $GITHUB_ENV
      - name: Checkout release files
        uses: actions/checkout@v4
        with:
          ref: ${{ env.GIT_VERSION }}
          path: ion
      - name: Download compiled launcher
        timeout-minutes: 35
        run: |
          curl --fail --location \
            --retry 30 \
            --retry-delay 60 \
            --retry-all-errors \
            --retry-max-time 1800 \
            --output ion-launcher.zip \
            "https://github.com/R2Ion/IonLauncher/releases/download/${GIT_VERSION}/ion-launcher.zip"
      - name: Download DiscordRPC plugin
        run: wget "https://github.com/R2Ion/IonDiscordRPC/releases/download/${{ env.DISCORD_RPC_VERSION }}/northstar-discord-rpc.zip"
      - name: Download compiled stubs
        run: wget "https://github.com/R2Ion/IonStubs/releases/download/v3/NorthstarStubs.zip"
      - name: Checkout core mods
        uses: actions/checkout@v4
        with:
          repository: R2Ion/IonMods
          ref: ${{ env.GIT_VERSION }}
          path: northstar-mods
      - name: Update mod version info
        run: |
          export MOD_VERSION=$(echo $NORTHSTAR_VERSION | tr --delete 'v')
          export PATCH_VERSION=$(echo $ION_PATCH)
          cat <<< $(jq ".Version = \"${MOD_VERSION}\"" northstar-mods/Northstar.Client/mod.json) > northstar-mods/Northstar.Client/mod.json
          cat <<< $(jq ".Version = \"${MOD_VERSION}\"" northstar-mods/Northstar.Custom/mod.json) > northstar-mods/Northstar.Custom/mod.json
          cat <<< $(jq ".Version = \"${MOD_VERSION}\"" northstar-mods/Northstar.CustomServers/mod.json) > northstar-mods/Northstar.CustomServers/mod.json
          cat <<< $(jq ".Patch = \"${PATCH_VERSION}\"" northstar-mods/Northstar.Client/mod.json) > northstar-mods/Northstar.Client/mod.json
          cat <<< $(jq ".Patch = \"${PATCH_VERSION}\"" northstar-mods/Northstar.Custom/mod.json) > northstar-mods/Northstar.Custom/mod.json
          cat <<< $(jq ".Patch = \"${PATCH_VERSION}\"" northstar-mods/Northstar.CustomServers/mod.json) > northstar-mods/Northstar.CustomServers/mod.json
      - name: Generate md5sum file
        working-directory: northstar-mods
        run: |
          echo -e "# Check with 'md5sum -c md5sum.txt' at the root of mods folder\n" > md5sum.txt
          git ls-tree -r $NORTHSTAR_VERSION --name-only | xargs -L1 md5sum >> md5sum.txt
      - name: Make folder structure
        run: |
          mkdir --parents ion/R2Northstar/mods
          mkdir --parents ion/R2Northstar/plugins
          mkdir --parents ion/bin/x64_dedi

          unzip northstar-discord-rpc.zip -d ion/R2Northstar/plugins

          unzip NorthstarStubs.zip -d ion/bin/x64_dedi
          unzip ion-launcher.zip -d ion
          rsync -avr --exclude="Northstar.Coop" --exclude=".git*" --exclude="*.md" northstar-mods/. ion/R2Northstar/mods
      - name: Checkout Navmesh repository
        uses: actions/checkout@v4
        with:
          repository: R2Ion/IonNavs
          ref: "v6"
          path: northstar-navs
      - name: Navmeshes setup
        run: |
          mkdir --parents ion/R2Northstar/mods/Northstar.CustomServers/mod/maps
          rsync -avr northstar-navs/graphs ion/R2Northstar/mods/Northstar.CustomServers/mod/maps/
          rsync -avr northstar-navs/navmesh ion/R2Northstar/mods/Northstar.CustomServers/mod/maps/
      - name: Cleanup root Ion repository files
        working-directory: ion
        run: |
          rm -rf .git .github .gitignore *.md LICENSE thunderstore .ci.env.example *.sh
      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: Ion.release.${{ env.NORTHSTAR_VERSION }}
          path: ion
      - name: Create zip to upload
        run: |
          cd ion/
          zip --recurse-paths --quiet Ion.release.${{ env.NORTHSTAR_VERSION }}.zip .
          mv Ion.release.${{ env.NORTHSTAR_VERSION }}.zip ../
      - name: Compute SHA-512 checksum
        id: checksum
        run: |
          echo "sha512=$(sha512sum Ion.release.${{ env.NORTHSTAR_VERSION }}.zip | awk '{print $1}')" >> $GITHUB_OUTPUT
      - name: Upload zip to release draft
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/v') && !contains(env.NORTHSTAR_VERSION, '-rc')
        with:
          name: Ion Release ${{ env.NORTHSTAR_VERSION }} Patch ${{ env.ION_PATCH }}
          draft: true
          files: Ion.release.${{ env.NORTHSTAR_VERSION }}.zip

  build-thunderstore-package:
    needs: build-northstar # comment out when running locally
    env:
      # Release envs, comment this out when running locally
      TCLI_AUTH_TOKEN: ${{ secrets.THUNDERSTORE_TOKEN }}
      TS_REPO: https://thunderstore.io/
      TS_COMMUNITY: northstar

      # Uncomment when testing locally, must be unique
      # NORTHSTAR_VERSION: 0.1.8

      # Thunderstore info and descriptions
      TS_NAMESPACE: r2ion
      TS_MOD_NAME: Ion
      TS_MOD_DESCRIPTION: Enhanced Northstar fork.
    runs-on: ubuntu-22.04
    steps:
      - name: Setup tcli
        run: |
          wget --output-document tcli.tar.gz https://github.com/thunderstore-io/thunderstore-cli/releases/download/0.2.4/tcli-0.2.4-linux-x64.tar.gz
          tar xvf tcli.tar.gz
          sudo mv --verbose tcli-0.2.4-linux-x64/tcli /bin
      - name: Set version variables
        run: |
          echo "NORTHSTAR_VERSION=${GITHUB_REF_NAME%_*}" >> $GITHUB_ENV
          echo "ION_PATCH=${GITHUB_REF_NAME##*_}" >> $GITHUB_ENV
      - name: (DEBUG) Download Northstar package
        if: ${{ env.ACT }} # Download Ion package from releases when running locally instead of relying on previous jobs
        run: |
          wget --output-document ion.zip https://github.com/R2Ion/Ion/releases/download/v1.6.3/Ion.release.v1.6.3.zip
          unzip ion.zip -d ion

      - name: Download Ion package
        if: ${{ !env.ACT }} # Download artifacts from previous jobs when running on GitHub's infrastructure
        uses: actions/download-artifact@v4
        with:
          name: Ion.release.${{ env.NORTHSTAR_VERSION }}
          path: ion

      - name: Make package structure
        run: |
          mkdir --parents thunderstore/dist/Ion
          mv --verbose ion/* thunderstore/dist/Ion
          wget --output-document thunderstore/icon.png https://raw.githubusercontent.com/R2Ion/Ion/main/thunderstore/icon.png
          wget --output-document thunderstore/README.md https://raw.githubusercontent.com/R2Ion/Ion/main/thunderstore/README.md

      - name: Setup environment variables
        run: |
          BASE_VERSION=$(echo $NORTHSTAR_VERSION | tr -d 'v'| sed --expression 's/-rc/0/' | sed -E 's/([0-9]+)\.([0-9]+)\.(0*)([0-9])/\1.\2.\4/;')
          LAST_COMPONENT=${BASE_VERSION##*.}
          PAD_LEN=$((6 - ${#LAST_COMPONENT}))
          if [ $PAD_LEN -lt 0 ]; then PAD_LEN=0; fi
          ION_PATCH_PADDED=$(printf "%0${PAD_LEN}d" "$ION_PATCH")
          echo "MOD_VERSION=${BASE_VERSION}${ION_PATCH_PADDED}" >> $GITHUB_ENV

          # If it's a release candidate we also want to change a few things
          if [[ $NORTHSTAR_VERSION == *"-rc"* ]]; then
            # We want to push to a different package
            # i.e. `NorthstarReleaseCandidate` vs `Northstar`
            echo "TS_MOD_NAME=IonReleaseCandidate" >> $GITHUB_ENV
            # And update mod description
            echo "TS_MOD_DESCRIPTION=Release candidate for next Ion release." >> $GITHUB_ENV
            # Add disclaimer add the top of README
            echo -e '> ⚠️ This is a release candidate. Please report bugs or other issues on GitHub ⚠️\n' | cat - thunderstore/README.md > temp && mv temp thunderstore/README.md
          fi

      - name: Publish package to Thunderstore
        working-directory: thunderstore
        run: |
          tcli init --package-name=$TS_MOD_NAME --package-namespace=$TS_NAMESPACE --package-version $MOD_VERSION

          sed --in-place "s/communities = \[\]/communities = [\"$TS_COMMUNITY\"]/g" thunderstore.toml
          sed --in-place "s/Example-Dependency = \"1.0.0\"//g" thunderstore.toml
          sed --in-place "s/description = \"Example mod description\"/description = \"$TS_MOD_DESCRIPTION\"/g" thunderstore.toml
          cat thunderstore.toml

          tcli build
          tcli publish --repository "$TS_REPO" --file build/*.zip

  # build-docker-image:
  #   needs: build-northstar
  #   runs-on: ubuntu-22.04

  #   steps:
  #     - name: Set version variables
  #       run: |
  #         echo "NORTHSTAR_VERSION=${GITHUB_REF_NAME%_*}" >> $GITHUB_ENV
  #         echo "ION_PATCH=${GITHUB_REF_NAME##*_}" >> $GITHUB_ENV
  #     - name: Download Ion package
  #       uses: actions/download-artifact@v4
  #       with:
  #         name: Ion.release.${{ env.NORTHSTAR_VERSION }}
  #         path: .

  #     - name: Repository dispatch
  #       uses: peter-evans/repository-dispatch@v3
  #       env:
  #         SHA512: ${{needs.build-northstar.outputs.sha512}}
  #       with:
  #         token: ${{ secrets.REPO_DISPATCH_TOKEN }}
  #         repository: R2Ion/ion-dedicated
  #         event-type: build-docker-image
  #         client-payload: '{"version": "${{ env.NORTHSTAR_VERSION }}", "patch": "${{ env.ION_PATCH }}", "sha512": "${{ needs.build-northstar.outputs.sha512 }}"}'
